# Optional local settings. Do not commit a filled .env file.

# Set to 1 only when you want pytest to touch Xiaohongshu.
XHS_LIVE_TEST=0
XHS_LIVE_KEYWORD=咖啡
XHS_LIVE_HEADLESS=true

# Optional named account profile. Keep blank for the legacy default profile.
XHS_PROFILE=
